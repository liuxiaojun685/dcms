package ${rootPackageName};

import ${rootPackageName}.exception.ApiException;
import ${rootPackageName}.model.*;

/**
 * 自动生成的API接口类，不要手动修改
 */
public interface ${className} {
    /**
     * ${description!}
     */
    ${responseName} execute(${requestName} ${requestName?uncap_first}) throws ApiException;
}
